package icu.takeneko.omms.client;

import icu.takeneko.omms.client.session.request.InitRequest;

public class Constants {
    public static final long PROTOCOL_VERSION = InitRequest.VERSION_BASE + 0x11;
}
